{
  "name": "@acds/persistence-pg",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "PostgreSQL persistence adapters and audit/event writers for ACDS runtime state.",
  "scripts": {
    "build": "tsc",
    "test": "node ../../scripts/run-package-tests.mjs persistence-pg",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pg": "^8.13.0",
    "@acds/core-types": "workspace:*",
    "@acds/provider-broker": "workspace:*",
    "@acds/execution-orchestrator": "workspace:*",
    "@acds/adaptive-optimizer": "workspace:*",
    "@acds/policy-engine": "workspace:*",
    "@acds/security": "workspace:*",
    "@acds/audit-ledger": "workspace:*",
    "@acds/evaluation": "workspace:*"
  },
  "devDependencies": {
    "@types/pg": "^8.11.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
