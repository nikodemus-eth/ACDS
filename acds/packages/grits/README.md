# @acds/grits

Shared GRITS integrity engine, checker logic, and snapshot types.

## Scripts

- `pnpm --filter @acds/grits run build`
- `pnpm --filter @acds/grits run test`
- `pnpm --filter @acds/grits run typecheck`

This package supplies the shared integrity model used by `@acds/grits-worker`.
