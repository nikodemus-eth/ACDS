{
  "name": "@acds/grits",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "description": "Shared GRITS integrity engine, checkers, and snapshot types for ACDS.",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@acds/core-types": "workspace:*",
    "@acds/adaptive-optimizer": "workspace:*",
    "@acds/audit-ledger": "workspace:*"
  }
}
