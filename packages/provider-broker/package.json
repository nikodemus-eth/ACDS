{
  "name": "@acds/provider-broker",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@acds/core-types": "workspace:*",
    "@acds/provider-adapters": "workspace:*",
    "@acds/security": "workspace:*",
    "@acds/audit-ledger": "workspace:*"
  }
}
